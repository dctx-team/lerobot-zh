# HopeJR

## 前提条件

- [硬件设置](https://github.com/TheRobotStudio/HOPEJr)

## 安装 LeRobot

遵循[安装说明](https://github.com/huggingface/lerobot#installation)来安装 LeRobot。

使用 HopeJR 依赖项安装 LeRobot：

```bash
pip install -e ".[hopejr]"
```

## 设备配置

在开始校准和操作之前，您需要识别每个 HopeJR 组件的 USB 端口。运行此脚本以查找机械臂、手、手套和外骨骼的 USB 端口：

```bash
lerobot-find-port
```

这将显示可用的 USB 端口及其关联的设备。记下端口路径（例如，`/dev/tty.usbmodem58760433331`、`/dev/tty.usbmodem11301`），因为在记录数据、回放片段或运行远程操作脚本时，您需要在 `--robot.port` 和 `--teleop.port` 参数中指定它们。

## 步骤 1：校准

在执行远程操作之前，需要校准 HopeJR 的肢体。校准文件将保存在 `~/.cache/huggingface/lerobot/calibration` 中

### 1.1 校准机器人手

```bash
lerobot-calibrate \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=blue \
    --robot.side=right
```

运行校准脚本时，将弹出一个校准 GUI。手指关节命名如下：

**拇指**：

- **CMC**：连接拇指到手掌的基础关节
- **MCP**：指关节
- **PIP**：第一个手指关节
- **DIP**：指尖关节

**食指、中指、无名指和小指**：

- **Radial flexor（桡侧屈肌）**：将手指基部向拇指方向移动
- **Ulnar flexor（尺侧屈肌）**：将手指基部向小指方向移动
- **PIP/DIP**：弯曲手指的远节和近节指骨

这些中的每一个都需要通过 GUI 单独校准。
请注意，尺侧和桡侧屈肌应具有相同大小的范围（但具有不同的偏移量），以获得对称运动。

<p align="center">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/calibration_gui_1.png"
    alt="在手部校准 GUI 中设置边界"
    title="在手部校准 GUI 中设置边界"
    width="100%"
  ></img>
</p>

使用校准界面为每个关节设置范围边界，如上所示。

<p align="center">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/calibration_gui_2.png"
    alt="保存校准值"
    title="保存校准值"
    width="100%"
  ></img>
</p>

为所有关节设置适当的边界后，单击"Save"（保存）将校准值保存到电机。

### 1.2 校准远程操作手套

```bash
lerobot-calibrate \
    --teleop.type=homunculus_glove \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=red \
    --teleop.side=right
```

从拇指开始，移动每个手指通过其全部运动范围。

```
Move thumb through its entire range of motion.
Recording positions. Press ENTER to stop...

-------------------------------------------
NAME      |    MIN |    POS |    MAX
thumb_cmc |   1790 |   1831 |   1853
thumb_mcp |   1497 |   1514 |   1528
thumb_pip |   1466 |   1496 |   1515
thumb_dip |   1463 |   1484 |   1514
```

继续每个手指：

```
Move middle through its entire range of motion.
Recording positions. Press ENTER to stop...

-------------------------------------------
NAME                 |    MIN |    POS |    MAX
middle_mcp_abduction |   1598 |   1718 |   1820
middle_mcp_flexion   |   1512 |   1658 |   2136
middle_dip           |   1484 |   1500 |   1547
```

校准完成后，系统将保存校准到 `/Users/your_username/.cache/huggingface/lerobot/calibration/teleoperators/homunculus_glove/red.json`

### 1.3 校准机器人手臂

```bash
lerobot-calibrate \
    --robot.type=hope_jr_arm \
    --robot.port=/dev/tty.usbserial-1110 \
    --robot.id=white
```

这将打开一个校准 GUI，您可以在其中为每个电机设置范围限制。手臂运动组织如下：

- **Shoulder（肩部）**：俯仰、偏航和滚转
- **Elbow（肘部）**：弯曲
- **Wrist（腕部）**：俯仰、偏航和滚转

<p align="center">
  <img
    src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot/calibration_gui_2.png"
    alt="在手臂校准 GUI 中设置边界"
    title="在手臂校准 GUI 中设置边界"
    width="100%"
  ></img>
</p>

使用校准界面为每个关节设置范围边界。移动每个关节通过其全部运动范围，并相应调整最小值和最大值。为所有关节设置适当的边界后，保存校准。

### 1.4 校准远程操作外骨骼

```bash
lerobot-calibrate \
    --teleop.type=homunculus_arm \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=black
```

外骨骼允许人控制机器人手臂。在校准期间，您将被提示移动所有关节通过其全部运动范围：

```
Move all joints through their entire range of motion.
Recording positions. Press ENTER to stop...

-------------------------------------------
-------------------------------------------
NAME            |    MIN |    POS |    MAX
shoulder_pitch  |    586 |    736 |    895
shoulder_yaw    |   1257 |   1374 |   1390
shoulder_roll   |    449 |   1034 |   2564
elbow_flex      |   3023 |   3117 |   3134
wrist_roll      |   3073 |   3096 |   3147
wrist_yaw       |   2143 |   2171 |   2185
wrist_pitch     |   1975 |   1993 |   2074
Calibration saved to /Users/your_username/.cache/huggingface/lerobot/calibration/teleoperators/homunculus_arm/black.json
```

## 步骤 2：远程操作

由于 Feetech 中间件中的全局变量冲突，手臂和手的远程操作必须在单独的 shell 会话中运行：

### 手

```bash
lerobot-teleoperate \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=blue \
    --robot.side=right \
    --teleop.type=homunculus_glove \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=red \
    --teleop.side=right \
    --display_data=true \
    --fps=30
```

### 手臂

```bash
lerobot-teleoperate \
    --robot.type=hope_jr_arm \
    --robot.port=/dev/tty.usbserial-1110 \
    --robot.id=white \
    --teleop.type=homunculus_arm \
    --teleop.port=/dev/tty.usbmodem11201 \
    --teleop.id=black \
    --display_data=true \
    --fps=30
```

## 步骤 3：记录、回放、训练

使用 Hope-JR 的记录、回放和训练仍处于实验阶段。

### 记录

此步骤记录数据集，可以在[这里](https://huggingface.co/datasets/nepyope/hand_record_test_with_video_data/settings)看到一个示例。

```bash
lerobot-record \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=right \
    --robot.side=right \
    --robot.cameras='{"main": {"type": "opencv", "index_or_path": 0, "width": 640, "height": 480, "fps": 30}}' \
    --teleop.type=homunculus_glove \
    --teleop.port=/dev/tty.usbmodem1201 \
    --teleop.id=right \
    --teleop.side=right \
    --dataset.repo_id=nepyope/hand_record_test_with_video_data \
    --dataset.single_task="Hand recording test with video data" \
    --dataset.num_episodes=1 \
    --dataset.episode_time_s=5 \
    --dataset.push_to_hub=true \
    --dataset.private=true \
    --display_data=true
```

### 回放

```bash
lerobot-replay \
    --robot.type=hope_jr_hand \
    --robot.port=/dev/tty.usbmodem58760432281 \
    --robot.id=right \
    --robot.side=right \
    --dataset.repo_id=nepyope/hand_record_test_with_camera \
    --dataset.episode=0
```

### 训练

```bash
lerobot-train \
  --dataset.repo_id=nepyope/hand_record_test_with_video_data \
  --policy.type=act \
  --output_dir=outputs/train/hopejr_hand \
  --job_name=hopejr \
  --policy.device=mps \
  --wandb.enable=true \
  --policy.repo_id=nepyope/hand_test_policy
```

### 评估

此训练运行可以在[这里](https://wandb.ai/tino/lerobot/runs/rp0k8zvw?nw=nwusertino)查看示例。

```bash
lerobot-record \
  --robot.type=hope_jr_hand \
  --robot.port=/dev/tty.usbmodem58760432281 \
  --robot.id=right \
  --robot.side=right \
  --robot.cameras='{"main": {"type": "opencv", "index_or_path": 0, "width": 640, "height": 480, "fps": 30}}' \
  --display_data=false \
  --dataset.repo_id=nepyope/eval_hopejr \
  --dataset.single_task="Evaluate hopejr hand policy" \
  --dataset.num_episodes=10 \
  --policy.path=outputs/train/hopejr_hand/checkpoints/last/pretrained_model
```
